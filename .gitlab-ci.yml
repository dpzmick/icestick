image: ubuntu

before_script:
  - apt-get update
  - apt-get install -y verilator
  - apt-get install -y clang
  - apt-get install -y make

test:
  script:
    - cd spi
    - make -j $(nproc) simtest
    - make -j $(nproc) tb
